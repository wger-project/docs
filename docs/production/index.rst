Installation (prod)
===================

It is *very* recommended to use the docker image for production as everything
is already set up and configured (it's also what we are using for the "official"
wger.de server). But you can of course also install wger and all the dependencies
manually.

.. toctree::
   :maxdepth: 1

   docker
   installation